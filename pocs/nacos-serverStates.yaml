id: nacos-version-info-leak
requests:

  - method: GET
    path: ["/nacos/v1/console/server/state"]
    matchers-condition: and
    matchers:
      - type: status
        status: [200]
      - type: word
        words: ["version", "standalone_mode", "function_mode"]
        condition: and